# Azure app registration client ID for Microsoft login (roadmap P0-02).
# This value is public by design (public-client OAuth app), not a secret.
# Until Mojang approves the app for the Minecraft API (P0-03), development
# logins fall back to msmc's default client ID.
AXO_MSA_CLIENT_ID=
